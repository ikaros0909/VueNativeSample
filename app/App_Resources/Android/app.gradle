// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "myapp.test"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
